# CLAUDE.md — Инструкции для Claude по проекту LaunchFlow

## Что это за проект

Продающий лендинг для SaaS-платформы **LaunchFlow** — сервиса, который помогает экспертам самостоятельно запускать продающие вебинары и эфиры без продюсера, дизайнера и технического специалиста.

**Создатель продукта:** Лиханова Олеся — эксперт по системным запускам, основатель LaunchFlow.

---

## О пользователе

- Не имеет технического фона, учится создавать проекты
- Полностью доверяет Claude в технических решениях
- Нужны объяснения: всегда объяснять ЧТО делается и ПОЧЕМУ
- После изменений кратко пояснять, что можно безопасно менять дальше

---

## Стек

- **React 18** + **Vite 6** + **Tailwind CSS 3**
- **Lucide React** — иконки
- Node.js установлен по пути `C:\Program Files\nodejs\`

## Команды

```bash
# Запуск локального сервера (открывает браузер автоматически)
npm run dev
# → http://127.0.0.1:3000 (компьютер)
# → http://192.168.0.134:3000 (телефон в той же Wi-Fi сети)

# Сборка для публикации
npm run build
# → создаёт папку dist/ — её перетаскивают на netlify.com/drop
```

**Для запуска без терминала:** двойной клик на `ЗАПУСТИТЬ САЙТ.bat`

---

## Архитектура

```
src/
├── components/
│   ├── ui/          — Button, Badge, SectionTitle
│   ├── layout/      — Header, Footer
│   └── sections/    — 11 секций лендинга
├── data/            — весь контент (тексты, цены, FAQ)
├── hooks/           — useScrollAnimation.js
├── App.jsx          — сборка секций
└── index.css        — стили и утилиты
```

## Секции (порядок в App.jsx)

1. `Hero` — главный экран
2. `Pain` — боли аудитории
3. `Solution` — решение (было/стало)
4. `Features` — возможности платформы
5. `HowItWorks` — 4 шага
6. `Creator` — блок Лихановой Олеси
7. `Benefits` — преимущества
8. `ForWhom` — для кого
9. `Pricing` — тарифы
10. `FAQ` — вопросы и ответы
11. `FinalCTA` — финальный призыв

---

## Цветовая палитра (tailwind.config.js)

| Переменная | Значение | Использование |
|---|---|---|
| `bg` | `#07070f` | Основной фон |
| `bg-surface` | `#0e0e1a` | Фон секций |
| `bg-card` | `#13131f` | Карточки |
| `primary` | `#7c3aed` | Фиолетовый акцент |
| `primary-light` | `#9f67ff` | Светлый фиолетовый |
| `gold` | `#d4a847` | Золотой акцент |
| `text` | `#f0f0f8` | Основной текст |
| `text-muted` | `#8888aa` | Приглушённый текст |

---

## Контент (что и где менять)

| Что | Файл |
|---|---|
| Боли аудитории | `src/data/pain.js` |
| Возможности платформы | `src/data/features.js` |
| Тарифы и цены | `src/data/pricing.js` |
| Вопросы и ответы | `src/data/faq.js` |
| Фото Олеси | `src/components/sections/Creator.jsx` |
| Навигация | `src/components/layout/Header.jsx` |
| Название сайта | `index.html` (title) и Header/Footer |

---

## Целевая аудитория продукта

Психологи, коучи, наставники, энергопрактики, преподаватели и эксперты, которые продают через эфиры, вебинары, мастер-классы, интенсивы и разборы.

---

## Чего НЕТ и не должно появляться

- Backend, авторизация, личный кабинет
- Интеграции (CRM, платёжки, email-сервисы)
- Многостраничность, блог, админка
- Тяжёлые анимации

---

## Публикация

Сайт публикуется на **Netlify** методом drag & drop:
1. `npm run build` → появляется папка `dist/`
2. Перетащить `dist/` на `netlify.com/drop`
3. Ссылка остаётся той же при повторных загрузках (если залогинена)

---

## Статус проекта

- [x] Лендинг создан (11 секций)
- [x] Node.js установлен
- [x] Локальный запуск настроен
- [x] Сборка работает
- [ ] Реальное фото Лихановой Олеси
- [ ] Финальные тексты и цены
- [ ] Форма заявки
- [ ] Публикация на Netlify
- [ ] Аналитика (Яндекс.Метрика)
